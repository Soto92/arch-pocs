server:
  port: 8080

spring:
  application:
    name: api-service
  datasource:
    url: jdbc:postgresql://localhost:5432/pipeline
    username: pipeline
    password: pipeline

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus